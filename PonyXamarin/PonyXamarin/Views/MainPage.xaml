<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="PonyXamarin.Views.MainPage"
             Title="MainPage"
             BackgroundColor="Purple">
  <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <ListView ItemsSource="{Binding ObservablePonies}" HasUnevenRows="True" VerticalOptions="StartAndExpand">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell TextColor="Black" Text="{Binding Name}" Detail="{Binding PonyStyle.Color}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Command="{Binding GoToForm}" Text="AGREGAR PONY" />
        <Button Command="{Binding GetPonies}" Text="REFRESH" />
        <Button Command="{Binding TruncatePonies}" Text="TRUNCATE"/>
    </StackLayout>
</ContentPage>