<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="PonyXamarin.Views.FormPage"
             BackgroundColor="Pink">
    
    <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="CenterAndExpand">

        <StackLayout Orientation="Horizontal">
            <Label Text="Name:" />
            <Entry Text="{Binding Pony.Name}" />
        </StackLayout>

        <StackLayout Orientation="Horizontal">
            <Label Text="Color:" />
            <Entry Text="{Binding Pony.PonyStyle.Color}" />
        </StackLayout>
        <Button Text="POST" Command="{Binding PostPony}"/>
    </StackLayout>
</ContentPage>